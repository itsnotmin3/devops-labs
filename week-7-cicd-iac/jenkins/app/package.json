{
  "name": "store-api",
  "version": "1.0.0",
  "description": "Tiny app so the Jenkins labs have something real to build",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "lint": "echo 'lint: no issues' && exit 0",
    "build": "mkdir -p dist && cp server.js dist/ && echo built ok"
  },
  "license": "MIT"
}
